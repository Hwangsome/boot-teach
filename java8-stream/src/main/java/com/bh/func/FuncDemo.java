package com.bh.func;

/**
 * Java内置的四大核心函数式接口
 * Consumer<T> ： void accept(T t); ==》对类型为T的对象应用操作
 * Supplier<T> ：T get(); ==》返回类型为T的对象
 * Function<T, R>：   R apply(T t); ==》对类型为T 的对象应用操作，并返回结果，结果时R类型的对象
 *  Predicate<T>： boolean test(T t);==》确定类型为T的对象是否满足某种约束，并返回boolean值
 *
 *
 *
 */
public class FuncDemo {
}
